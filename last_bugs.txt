quelques PIPE qui leaks
env -i ./minishell + cd ~ = segfault            OK
si enormement de commandes = ressources unvailable mais casse tout "l'ordi"
voir export test=                               OK
export test="echo user is :$USER" -> $test marche pas
export -> ne close pas le processus     ok
env -i -> mettre les 3 variables de bases OK

export A="echo user is :$USER" = print mais sans espaces

Pas de code erreur sur le here_doc
exit | exit 123 njnjn 123 = pas de message d'erreur
cat ctrl+C = pas de ^C
toujours pipe qui leaks

< cat Makefile > out | < out cat -e | grep $ > out3 > out4 > out 5 = reste bloqué

< Makefile cat > out | < out cat -e | grep $ > out1 > out 2 > out3 = pas de msg d'erreur

CHECKER LE MAKEFILE







export
pqs de check valid pu non genre var = ¢ffff devrait pas etre valid
devrait etre 
premiere char, soit une lettre min, maj soit _
ensuite, peut etre lettre, chiffre, _ 


export fff="'ech     o"

variable avec > ou < genre echo >$USER : crer un ficher $USER et non un fichier tdeville

les erreurs des bluildtin pqs retourner dqns le $?

heredoc + ctrl c => $? = 0 alors que devrait etre 1

signaux :
crtl + \ dqns heredoc quitte => devrait rien faire

echo>in< out gggggg ne met rien dans in alors que devrait mettre gggggg
alors que cat>in < out marche bien 

leaks de fd entre les commandes


si cat <fff avec fff un repertoire, le $? = 255, il devrait etre de 1


EN PRIORITÉ:
    MOI - heredoc + ctrl c => $? = 0 alors que devrait etre 1
    MOI- les erreurs des bluildtin pqs retourner dqns le $?
    MOI - crtl + \ dqns heredoc quitte => devrait rien faire
    MOI - echo>in< out gggggg ne met rien dans in alors que devrait mettre gggggg
    MOI - virer tous les prinfs et les dprintfs