1 - PIPE LEAKS
2 - FD LEAKS ENTRE COMMANDES
3 - si enormement de commandes = ressources unvailable mais casse
    tout "l'ordi" fork = -1 puis return donc n'atteint pas les waitpid      DONE
4 - CHECKER LE MAKEFILE

-------> si on veut se faire vraiment chier faire export fff="'ech     o"

-------> et ce truc aussi mais la c pour le plaisir de la ride variable avec > ou < genre echo >$USER : crer un ficher $USER et non un fichier tdeville



EN PRIORITÉ:
    MOI - affice les signaux quand il faut pas ctrl + \ dans le heredoc             -> Marche nickel a l'école
    MOI - ctrl + c doit etre ignorer quand on est dans le prompt sans commandes     -> Marche nickel a l'école
    MOI - G_STATUS faire quelques checks;
    MOI - < $USER crée un fichier nommé $USER au lieu de la variable d'environment\;
    Leaks de pipe sur heredoc
    close tous les files sur les redirections

doit faire: CTRL+C dans un heredoc, exit l'excution (pas d'ouverture des autres redirs et heredocs)
$? ne marche pas (il doit marcher comme une variable d'env)
