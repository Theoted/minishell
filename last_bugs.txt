quelques PIPE qui leaks
si enormement de commandes = ressources unvailable mais casse tout "l'ordi"
export -> ne close pas le processus     ok

export A="echo user is :$USER" = print mais sans espaces
Pas de code erreur sur le here_doc
exit | exit 123 njnjn 123 = pas de message d'erreur
cat ctrl+C = pas de ^C
toujours pipe qui leaks

< Makefile cat > out | < out cat -e | grep "$" > out1 > out 2 > out3 = pas de msg d'erreur

CHECKER LE MAKEFILE


export fff="'ech     o"

variable avec > ou < genre echo >$USER : crer un ficher $USER et non un fichier tdeville


heredoc + ctrl c => $? = 0 alors que devrait etre 1

signaux :
crtl + \ dqns heredoc quitte => devrait rien faire

echo>in< out gggggg ne met rien dans in alors que devrait mettre gggggg
alors que cat>in < out marche bien 

leaks de fd entre les commandes

EN PRIORITÉ:
    MOI - heredoc + ctrl c => $? = 0 alors que devrait etre 1   A faire
    A rechecker j'ai peut etre oublié de faire des tests- les erreurs des bluildtin pqs retourner dqns le $?
    MOI - crtl + \ dqns heredoc quitte => devrait rien faire
    MOI - Exit | exit err;